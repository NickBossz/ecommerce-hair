# ===========================================
# VARIÁVEIS OBRIGATÓRIAS
# ===========================================

# MongoDB Atlas - Obtenha em: MongoDB Atlas → Connect → Connection String
MONGODB_URI=
MONGODB_DB=ecommerce

# JWT Secret - Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# ===========================================
# VARIÁVEIS OPCIONAIS (já tem defaults)
# ===========================================

# Expiração do JWT (padrão: 7d)
# JWT_EXPIRES_IN=7d

# Ambiente (padrão: production)
# NODE_ENV=development

# URLs do Frontend/Admin para CORS (padrão: localhost)
# FRONTEND_URL=http://localhost:5173
# ADMIN_URL=http://localhost:5174
